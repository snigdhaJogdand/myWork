import { ColumnNameFilterPipe } from './column-name-filter.pipe';

describe('ColumnNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
