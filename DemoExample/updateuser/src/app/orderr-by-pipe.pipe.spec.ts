import { OrderrByPipePipe } from './orderr-by-pipe.pipe';

describe('OrderrByPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderrByPipePipe();
    expect(pipe).toBeTruthy();
  });
});
